[project]
name = "prom-monitor"
version = "0.1.0"
description = "Displays Fail2Ban metrics from Prometheus in the terminal"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["dotenv>=0.9.9", "prometheus-api-client>=0.7.2", "rich>=15.0.0"]
